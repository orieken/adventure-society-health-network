services:
  postgres:
    image: postgres:16
    ports:
      - "5432:5432"
    environment:
      POSTGRES_DB: ashn
      POSTGRES_USER: ashn_user
      POSTGRES_PASSWORD: ashn_password
    volumes:
      - ashn-postgres:/var/lib/postgresql/data

volumes:
  ashn-postgres:

